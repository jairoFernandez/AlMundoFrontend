import { DefaultUrlDirective } from './default-url.directive';

describe('DefaultUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultUrlDirective();
    expect(directive).toBeTruthy();
  });
});
